#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int t;
    scanf("%d", &t);
    for(int num = 0; num < t; num++)
        {
        int n;
        scanf("%d", &n);
        printf("%d\n", n + 1);
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
